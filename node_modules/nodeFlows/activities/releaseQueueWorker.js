
exports.executeActivity = executeActivity;

//Releases the queueWorker so that it will process other work
function executeActivity(workflow, args, callback) {

         workflow.getQueueWorker().releaseQueueWorker("Released");
         workflow.status = "waiting";
         workflow.getQueueItem().status = "waiting";
         callback({ success: false });
  

}
