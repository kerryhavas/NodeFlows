
exports.executeActivity = executeActivity;

function executeActivity(workflow, args, callback) {

    workflow.addHistory({ task: "WaitForTimeout",notes:"Timeout:" + args.timeout });
    setTimeout(function () {
        workflow.addHistory({ task: "ResumeFromTimeout" });
        callback({success: true, activity: "waitForTimeout"});
    }, args.timeout);


}
